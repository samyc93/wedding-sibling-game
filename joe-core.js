const screen=document.getElementById('screen'),progress=document.getElementById('progress'),bar=document.getElementById('bar'),toast=document.getElementById('toast');let level=0;const total=6;function setProgress(n,label){level=n;progress.textContent=label||`Level ${n} of ${total}`;bar.style.width=`${Math.max(0,Math.min(100,n/total*100))}%`}function showToast(msg){toast.textContent=msg;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1500)}function confetti(){const bits=['🐢','💚','⭐','🎉','🌴'];for(let i=0;i<32;i++){const e=document.createElement('span');e.className='confetti';e.textContent=bits[i%bits.length];e.style.left=Math.random()*100+'vw';e.style.animationDelay=Math.random()*.8+'s';document.body.appendChild(e);setTimeout(()=>e.remove(),3800)}}function button(label,fn,alt=false){const b=document.createElement('button');b.className='btn'+(alt?' alt':'');b.textContent=label;b.onclick=fn;return b}function renderIntro(){setProgress(0,'Ready');screen.innerHTML='<div class="eyebrow">A special mission</div><div class="hero">🐢</div><h1>Joe’s Turtle Quest</h1><p>Six challenges stand between you and one very important question.</p><p class="small">No turtles were harmed. A few burritos may be eaten.</p>';screen.append(button('Begin the quest',renderFind))}window.Joe={screen,setProgress,showToast,confetti,button,renderIntro};document.addEventListener('DOMContentLoaded',renderIntro);